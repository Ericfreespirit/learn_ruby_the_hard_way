- printf: formats and print its argument
> def sandwich (viande)->argument
return viande
end

- format: characters string with 3 types of objects (plain characters, escape sequence, format specification)
